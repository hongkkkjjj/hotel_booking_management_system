
class RoomData {
  int id;
  double price;
  DateTime date;

  RoomData(this.id, this.price, this.date);
}

class RoomType {
  int id;
  String title;
  String description;

  RoomType(this.id, this.title, this.description);
}